import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Consumer app smoke test', (WidgetTester tester) async {
    // TODO: Add integration tests for consumer flows
    expect(1 + 1, equals(2));
  });
}
